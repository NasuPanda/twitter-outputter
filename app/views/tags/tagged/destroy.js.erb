<% if @status == 'success' %>
  $("#<%= tagged_tag_id(@tag) %>").remove();
  // コードからイベントを発火させる
  document.getElementById("editing-post-content").dispatchEvent(new Event("input"));
<% elsif @status == 'failure' %>
  alert("セットしたハッシュタグの削除に失敗しました。既に削除されている可能性があります。");
<% end %>