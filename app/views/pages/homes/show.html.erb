<div class="container-fluid">
  <% if logged_in? %>
    <div class="row">
      <div class="col-2"></div>
      <div class="col">
        <%= render 'tags' %>
        <%= render 'post_form' %>
      </div>
      <div class="col-2"></div>
    </div>
  <% else %>
    <h1>ようこそ</h1>
  <% end %>
</div>