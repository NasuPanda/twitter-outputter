<li class="mb-1" id=<%= tag_id(tag) %> >
  <div class="tag-container">
    <%= link_to "# #{tag.name}", editing_path(tag_name: tag.name), class: "tag-name", method: :put, remote: true %>
    <%= link_to "︙", edit_tag_path(tag), class: "tag-edit", remote: true %>
  </div>
</li>