<ul id="tags-sidebar">
  <%# TODO @userはログイン時のみ持ってくるようにする %>
  <%= render partial: 'shared/tag', collection: current_user.tags %>
</ul>
<%= link_to '+', new_tag_path, remote: true, class: "btn btn-lg btn-primary"%>